interface:
  display_name: "Stremdbc"
  short_description: "Repo-specific patterns and workflows for stremdbc"
  default_prompt: "Use the stremdbc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true